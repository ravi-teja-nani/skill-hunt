export const Header = ({
    name
}) => {
    return <header  className="screen-heading">
        <div className="screen-heading-title">
            {
              name
            }
        </div>
    </header>
}